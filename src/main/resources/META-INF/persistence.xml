<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
    version="2.2">
    <!-- Configuração principal utilizando PostgreSQL em ambiente local/produção -->
    <persistence-unit name="tarefasPU" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <class>pedroleonez.gerenciadordetarefasjsf.model.Tarefa</class>
        <properties>
            <!-- Heroku / Render Database configuração -->
            <property name="jakarta.persistence.jdbc.driver" value="org.postgresql.Driver" />
            <property name="jakarta.persistence.jdbc.url" value="${JDBC_DATABASE_URL}" />
            <property name="jakarta.persistence.jdbc.user" value="${JDBC_DATABASE_USERNAME}" />
            <property name="jakarta.persistence.jdbc.password" value="${JDBC_DATABASE_PASSWORD}" />

            <!-- Hibernate Dialect para PostgreSQL -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.PostgreSQLDialect" />

            <!-- Hibernate -->
            <property name="hibernate.hbm2ddl.auto" value="update" />
            <property name="hibernate.show_sql" value="true" />
            <property name="hibernate.format_sql" value="true" />
        </properties>

    </persistence-unit>
</persistence>